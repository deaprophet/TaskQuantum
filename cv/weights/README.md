# Model Weights

This project uses SIFT (Scale-Invariant Feature Transform), which is a classical computer vision algorithm that does not require training.

**No model weights needed.**

SIFT parameters are configured in the code.
See `algorithm.py` for implementation details.
